{
  "name": "pilla",
  "version": "0.1.0",
  "description": "Maintain patches on top of pinned upstream git repos",
  "type": "module",
  "license": "MIT",
  "bin": {
    "pilla": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/cli.ts --outfile dist/cli.js --target node --minify",
    "bundle": "bun build src/cli.ts --compile --outfile dist/pilla",
    "dev": "bun src/cli.ts",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "eslint": "^9.0.0",
    "eslint-plugin-v3xlabs": "^1.7.14",
    "typescript": "^5.0.0"
  }
}
